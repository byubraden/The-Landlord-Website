<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Landlord Review Page</title>
    <style>
        /* General body style */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #F5F5F5; /* Light gray background */
            display: flex;
            justify-content: space-between;
        }

        /* Reviews section */
        .reviews-container {
            width: 66.6%;
            background-color: #FFFFFF; /* White background for reviews */
            padding: 20px;
            box-sizing: border-box;
            overflow-y: auto;
            height: 100vh;
        }

        .review {
            background-color: #E9F1F7; /* Light blue background for reviews */
            padding: 20px;
            margin-bottom: 15px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .review p {
            margin: 10px 0;
        }

        .review-text {
            font-size: 1.5rem;
            color: #004A66; /* Dark blue text */
            margin-bottom: 5px;
        }

        .review-date {
            font-size: 1rem;
            color: #555;
            margin-bottom: 15px;
        }

        .rating-summary {
            margin-top: 15px;
            background-color: #F2F9FD; /* Light blue background for rating summary */
            padding: 10px;
            border-radius: 8px;
            font-size: 1.1rem;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .summary-title {
            font-weight: bold;
            margin-bottom: 8px;
        }

        .category-summary {
            margin-bottom: 8px;
        }

        .average-rating {
            margin-top: 10px;
            font-weight: bold;
        }

        /* Rating section */
        .rating-container {
            width: 33.3%;
            background-color: #FFFFFF; /* White background for ratings */
            padding: 20px;
            box-sizing: border-box;
            height: 100vh;
            overflow-y: auto;
        }

        .landlord-name {
            font-size: 2rem;
            color: #004A66; /* Dark blue for landlord name */
            font-weight: bold;
            margin-bottom: 40px;
        }

        .rating-category {
            margin-bottom: 25px;
        }

        .category-name {
            font-size: 1.6rem;
            font-weight: bold;
            margin-bottom: 10px;
            color: #004A66; /* Dark blue for category names */
        }

        .progress-bar {
            height: 25px;
            background-color: #E9F1F7; /* Light blue background for progress bar */
            border-radius: 12px;
            overflow: hidden;
            margin-bottom: 10px;
        }

        .progress {
            height: 100%;
            background-color: #1B9AAA; /* Medium blue for the progress bar */
            width: 0%;
            border-radius: 12px;
        }

        .total-rating {
            font-size: 2rem;
            font-weight: bold;
            color: #004A66; /* Dark blue for the total rating */
            margin-top: 30px;
        }

        .progress-bar-container {
            margin-top: 30px;
        }

        /* Style for Add Review button */
        .add-review-button {
            display: block;
            margin-top: 40px;
            padding: 15px 20px;
            font-size: 1.2rem;
            font-weight: bold;
            color: #FFFFFF; /* White text on button */
            background-color: #1B9AAA; /* Medium blue for button */
            text-align: center;
            border-radius: 8px;
            text-decoration: none;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s ease;
        }

        .add-review-button:hover {
            background-color: #158B8A; /* Darker blue when hovered */
        }
    </style>
</head>

<body>
    <div class="reviews-container">
        <% if (reviews.length> 0) { %>
            <% reviews.forEach(review=> { %>
                <div class="review">
                    <div class="review-text"><strong>
                            <%= review.reviewer_first %>
                                <%= review.reviewer_last %>:
                        </strong>
                        <%= review.additional_comments || 'No comments provided' %>
                    </div>
                    <div class="review-date">Reviewed on: <%= new Date(review.date_created).toLocaleDateString() %>
                    </div>
                    <div class="rating-summary">
                        <div class="summary-title">Category Ratings:</div>
                        <div class="category-summary">Responsiveness: <%= review.responsiveness %>/5</div>
                        <div class="category-summary">Maintenance: <%= review.maintenance_and_repairs %>/5</div>
                        <div class="category-summary">Communication: <%= review.communication %>/5</div>
                        <div class="category-summary">Transparency: <%= review.transparency_and_honesty %>/5</div>
                        <div class="category-summary">Flexibility: <%= review.fairness_and_flexibility %>/5</div>
                    </div>
                </div>
                <% }); %>
                    <% } else { %>
                        <p>No reviews found for this landlord.</p>
                        <% } %>
    </div>

    <div class="rating-container">
        <div class="landlord-name">Landlord: <%= landlord %>
        </div>



        <!-- Responsiveness Rating -->
        <div class="rating-category">
            <div class="category-name">Responsiveness</div>
            <div class="progress-bar">
                <div class="progress" style="width: <%= averages.responsiveness ? averages.responsiveness * 20 : 0 %>%">
                </div>
            </div>
        </div>

        <!-- Maintenance and Repairs Rating -->
        <div class="rating-category">
            <div class="category-name">Maintenance and Repairs</div>
            <div class="progress-bar">
                <div class="progress" style="width: <%= averages.maintenance ? averages.maintenance * 20 : 0 %>%"></div>
            </div>
        </div>

        <!-- Communication Rating -->
        <div class="rating-category">
            <div class="category-name">Communication</div>
            <div class="progress-bar">
                <div class="progress" style="width: <%= averages.communication ? averages.communication * 20 : 0 %>%">
                </div>
            </div>
        </div>

        <!-- Transparency Rating -->
        <div class="rating-category">
            <div class="category-name">Transparency</div>
            <div class="progress-bar">
                <div class="progress" style="width: <%= averages.transparency ? averages.transparency * 20 : 0 %>%">
                </div>
            </div>
        </div>

        <!-- Flexibility Rating -->
        <div class="rating-category">
            <div class="category-name">Flexibility</div>
            <div class="progress-bar">
                <div class="progress" style="width: <%= averages.flexibility ? averages.flexibility * 20 : 0 %>%"></div>
            </div>
        </div>

        <!-- Overall Rating -->
        <div class="total-rating">
            Overall Rating: <%= totalAverage %>/5
        </div>

        <form action="/add-review/<%= landlordId %>" method="GET">
            <button type="submit" class="add-review-button">Add Review</button>
        </form>
    </div>


</body>

</html>